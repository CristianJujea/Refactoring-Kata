public final class QualityManager {
    private static QualityManager qualityManager = new QualityManager();
    
    private QualityManager() {}
    
    public static QualityManager getQualityManager() {
        return qualityManager;
    }
    
    // This class is a Facade to classes with business rules to update the quality of items.
    // Depeding on the short-term and on the long-term objectives of the business, 
    // this class may compose business rules by a Factory or by a Chain of Responsibility design pattern.

    
    public static Item updateItemQuality(Item itemToBeUpdated) {}
    public static Item[] updateItemsQuality(Item[] itemsToBeUpdated) {}
    
}
